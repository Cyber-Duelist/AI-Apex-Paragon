// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://cyber-duelist.github.io',
  base: '/AI-Apex-Paragon/',
  build: {
    assets: 'assets',
  },
});
